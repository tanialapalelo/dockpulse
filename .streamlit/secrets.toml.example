# Copy to .streamlit/secrets.toml (git-ignored) OR set in Streamlit Cloud > App > Settings > Secrets.
# The app reads GEMINI_API_KEY from the environment; Streamlit Cloud exposes secrets as env vars.
GEMINI_API_KEY = "your-google-ai-studio-key"
GEMINI_MODEL = "gemini-1.5-flash"
